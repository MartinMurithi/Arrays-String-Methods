//Removes white spaces before and after a string

const trim = (str) => {
    const newStr = str.trim();
    return newStr;
}

console.log(trim('             I am a software engineer               '));