// Unshift, inserts new element at the beginning of the array

const unshiftElement = () => {
    let fruits = ["mango", "Banana", "Orange"]
    fruits.unshift("Apple");
    return fruits
}

console.log(unshiftElement());
